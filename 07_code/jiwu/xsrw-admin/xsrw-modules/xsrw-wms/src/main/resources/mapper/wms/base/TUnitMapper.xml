<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xsrw.wms.base.mapper.TUnitMapper">

    <sql id="selectTUnitVo">
        select tunit.id, tunit.code, tunit.name, tunit.remark, tunit.create_by, tunit.create_time, tunit.update_by, tunit.update_time, tunit.dept_id, tunit.dept_name, tunit.del_flag
        from t_unit tunit
    </sql>

    <select id="selectTUnitList" parameterType="com.xsrw.wms.base.domain.TUnit" resultType="com.xsrw.wms.base.domain.TUnit">
        <include refid="selectTUnitVo"/>
        <if test="params.dataScope != null  and params.dataScope != ''">
            inner join sys_user u on u.user_name = tunit.create_by and u.status = '0'
            inner join sys_dept d on u.dept_id = d.dept_id and d.status = '0'
        </if>
        <where>
            tunit.del_flag = '0'
            <if test="name != null  and name != ''"> and tunit.name like concat('%', #{name}, '%')</if>
        </where>
        <!-- 数据范围过滤 -->
        ${params.dataScope}
        order by tunit.create_time desc
    </select>

    <delete id="deleteTUnitById" parameterType="Long">
         update t_unit set del_flag = '1' where id = #{id}
    </delete>

    <delete id="deleteTUnitByIds" parameterType="String">
        update t_unit set del_flag = '1' where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>
