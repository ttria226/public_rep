<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xsrw.wms.inout.mapper.TOutRemovalDetailMapper">

    <sql id="selectTOutRemovalDetailVo">
        select a.id, a.detail_origin_id, a.return_count, a.status, a.remark, a.create_by, a.create_time, a.update_by, a.update_time, a.dept_id, a.dept_name, a.del_flag
        from t_out_removal_detail a
    </sql>

    <select id="selectTOutRemovalDetailList" parameterType="com.xsrw.wms.inout.domain.TOutRemovalDetail"
            resultType="com.xsrw.wms.inout.domain.TOutRemovalDetail">
        <include refid="selectTOutRemovalDetailVo"/>
        <if test="params.dataScope != null  and params.dataScope != ''">
            inner join sys_user u on u.user_name = a.create_by and u.status = '0'
            inner join sys_dept d on u.dept_id = d.dept_id and d.status = '0'
        </if>
        <where>
            a.del_flag = '0'
            <if test="detailOriginId != null ">and a.detail_origin_id = #{detailOriginId}</if>
            <if test="returnCount != null ">and a.return_count = #{returnCount}</if>
            <if test="status != null  and status != ''">and a.status = #{status}</if>
        </where>
        <!-- 数据范围过滤 -->
        ${params.dataScope}
    </select>

    <delete id="deleteTOutRemovalDetailById" parameterType="Long">
         update t_out_removal_detail set del_flag = '1' where id = #{id}
    </delete>

    <delete id="deleteTOutRemovalDetailByIds" parameterType="String">
        update t_out_removal_detail set del_flag = '1' where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>
