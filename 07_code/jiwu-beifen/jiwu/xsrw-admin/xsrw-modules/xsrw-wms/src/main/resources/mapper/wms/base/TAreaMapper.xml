<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xsrw.wms.base.mapper.TAreaMapper">

    <sql id="selectTAreaVo">
        select tarea.id, tarea.name, tarea.status, tarea.remark, tarea.create_by, tarea.create_time, tarea.update_by, tarea.update_time, tarea.dept_id, tarea.dept_name, tarea.del_flag,
            tarea.code
        from t_area tarea
    </sql>

    <select id="selectTAreaList" parameterType="com.xsrw.wms.base.domain.TArea" resultType="com.xsrw.wms.base.domain.vo.TAreaVO">
        <include refid="selectTAreaVo"/>
        <if test="params.dataScope != null  and params.dataScope != ''">
            inner join sys_user u on u.user_name = tarea.create_by and u.status = '0'
            inner join sys_dept d on u.dept_id = d.dept_id and d.status = '0'
        </if>
        <where>
            tarea.del_flag = '0'
            <if test="name != null  and name != ''">and tarea.name like concat('%', #{name}, '%')</if>
            <if test="status != null  and status != ''">and tarea.status = #{status}</if>
        </where>
        <!-- 数据范围过滤 -->
        ${params.dataScope}
        order by tarea.create_time desc
    </select>

    <delete id="deleteTAreaById" parameterType="Long">
         update t_area set del_flag = '1' where id = #{id}
    </delete>

    <delete id="deleteTAreaByIds" parameterType="String">
        update t_area set del_flag = '1' where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>
