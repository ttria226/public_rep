<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xsrw.wms.base.mapper.TCategoryMapper">

    <sql id="selectTCategoryVo">
        select tcategory.id, tcategory.name, tcategory.remark, tcategory.create_by, tcategory.create_time, tcategory.update_by, tcategory.update_time, tcategory.dept_id, tcategory.dept_name, tcategory.del_flag
        from t_category tcategory
    </sql>

    <select id="selectTCategoryList" parameterType="com.xsrw.wms.base.domain.TCategory" resultType="com.xsrw.wms.base.domain.TCategory">
        <include refid="selectTCategoryVo"/>
        <if test="params.dataScope != null  and params.dataScope != ''">
            inner join sys_user u on u.user_name = tcategory.create_by and u.status = '0'
            inner join sys_dept d on u.dept_id = d.dept_id and d.status = '0'
        </if>
        <where>
            tcategory.del_flag = '0'
            <if test="name != null  and name != ''"> and tcategory.name like concat('%', #{name}, '%')</if>
        </where>
        <!-- 数据范围过滤 -->
        ${params.dataScope}
        order by tcategory.create_time desc
    </select>

    <delete id="deleteTCategoryById" parameterType="Long">
         update t_category set del_flag = '1' where id = #{id}
    </delete>

    <delete id="deleteTCategoryByIds" parameterType="String">
        update t_category set del_flag = '1' where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>
